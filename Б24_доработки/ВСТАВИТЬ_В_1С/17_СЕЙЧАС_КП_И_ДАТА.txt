================================================================================
СЕЙЧАС: КП + ДАТА ПЕРЕД ФАКСИМИЛЕ — ЧТО ВСТАВИТЬ
================================================================================

─── КП (кнопка) — 4 модуля ───

11 → Б24КП_Клиент              (Клиент)
12 → Б24КП_ВызовСервера         (Сервер, Вызов сервера, Привилегированный)
13 → Б24КП_ПечатьСервер         (Сервер)
14 → Б24КП_ИнтеграцияB24        (Сервер, Привилегированный)  ← B латинская!

Форма заказа — процедура (одна, без дублей):

&НаКлиенте
Процедура Б24_СформироватьКП(Команда)
	Б24КП_Клиент.СформироватьКП(ЭтотОбъект);
КонецПроцедуры

Команда Б24_СформироватьКП → Действие = Б24_СформироватьКП (Клиент, НЕ «После/Перед»)

─── Дата перед факсимиле — 3 модуля + 2 вставки ───

07 → Б24_СчетСервер             (Сервер) — уже есть
15 → Б24_СчетВызовСервера       (Сервер, Вызов сервера)
16 → Б24_ПечатьКлиент           (Клиент)

10 → заимствовать УправлениеПечатьюКлиент из УТ → в конец модуля расширения
08 → в конец модуля формы заказа (запасной перехват)

06 → ПечатьДокументов (запасная проверка в окне печати)

F7 → перезапуск 1С

================================================================================
