// МОДУЛЬ: заимствовать из основной УТ → Общий модуль УправлениеПечатьюКлиент
// Вставить эту область в КОНЕЦ модуля расширения (не заменять типовой код!).
//
// + создать модули: Б24_ПечатьКлиент (Клиент), Б24_СчетВызовСервера (Сервер, Вызов сервера)

#Область Б24_БлокДатыПередФаксимиле

&Вместо("ВыполнитьКомандуПечати")
Процедура Б24_ВместоВыполнитьКомандуПечати(ОписаниеКоманды, Форма)

	Если Б24_ПечатьКлиент.ЭтоОписаниеПечатиФаксимилеСчета(ОписаниеКоманды) Тогда

		ЗаказСсылка = Б24_ПечатьКлиент.ЗаказСсылкаИзФормыПечати(Форма, ОписаниеКоманды);
		ТекстОшибки = Б24_ПечатьКлиент.ТекстОшибкиДатыПередФаксимиле(ЗаказСсылка);

		Если НЕ ПустаяСтрока(ТекстОшибки) Тогда
			ПоказатьПредупреждение(, ТекстОшибки);
			Возврат;
		КонецЕсли;

	КонецЕсли;

	ПродолжитьВызов(ОписаниеКоманды, Форма);

КонецПроцедуры

#КонецОбласти
