// МОДУЛЬ: заимствовать из УТ → Общий модуль УправлениеПечатьюКлиент
// Вставить эту область в КОНЕЦ модуля расширения (типовой код НЕ копировать!).
//
// ВАЖНО: у процедуры 5 параметров + &НаКлиенте (не 2 параметра «ОписаниеКоманды, Форма»).
// Нужен модуль Б24_СчетВызовСервера (Сервер, Вызов сервера) и Б24_СчетСервер (Сервер).
//
// Плюс к блокировке даты: после успешной печати счёта (с факсимиле ИЛИ обычного) — тот же
// PDF автоматически уходит в таймлайн привязанной сделки B24, как и с кнопкой «Сформировать КП».

#Область Б24_БлокДатыПередФаксимиле

&НаКлиенте
&Вместо("ВыполнитьКомандуПечати")
Процедура Б24_ВместоВыполнитьКомандуПечати(ИмяМенеджераПечати, ИменаМакетов, МассивОбъектов, ВладелецФормы, ПараметрыПечати)

	ЗаказСсылка = Б24_ЗаказДляПроверкиДаты(МассивОбъектов, ВладелецФормы);

	Если Б24_ЭтоПечатьСчетаСФаксимиле(ИменаМакетов, ИмяМенеджераПечати) Тогда

		Если НЕ ЗначениеЗаполнено(ЗаказСсылка) Тогда
			ПоказатьПредупреждение(, "Сначала запишите заказ клиента.");
			Возврат;
		КонецЕсли;

		ТекстОшибки = Б24_СчетВызовСервера.ТекстОшибкиДатыПередСчетомСФаксимиле(ЗаказСсылка);

		Если НЕ ПустаяСтрока(ТекстОшибки) Тогда
			ПоказатьПредупреждение(, ТекстОшибки);
			Возврат;
		КонецЕсли;

	КонецЕсли;

	ПродолжитьВызов(ИмяМенеджераПечати, ИменаМакетов, МассивОбъектов, ВладелецФормы, ПараметрыПечати);

	// Печать открылась — если это был счёт, отправляем ту же печатную форму в таймлайн сделки B24.
	Если ЗначениеЗаполнено(ЗаказСсылка) Тогда

		РезультатB24 = Б24_СчетВызовСервера.ОтправитьСчетВТаймлайнЕслиЭтоСчет(
			ЗаказСсылка, ИмяМенеджераПечати, ИменаМакетов, МассивОбъектов, ПараметрыПечати);

		Если РезультатB24.Обрабатывалось И НЕ ПустаяСтрока(РезультатB24.ТекстСообщения) Тогда
			Если РезультатB24.Успех Тогда
				ПоказатьОповещениеПользователя("Счёт → Bitrix24", , РезультатB24.ТекстСообщения);
			Иначе
				ПоказатьОповещениеПользователя("Счёт → Bitrix24 (не удалось)", , РезультатB24.ТекстСообщения);
			КонецЕсли;
		КонецЕсли;

	КонецЕсли;

КонецПроцедуры

&НаКлиенте
Функция Б24_ЭтоПечатьСчетаСФаксимиле(ИменаМакетов, ИмяМенеджераПечати)

	Текст = НРег(СокрЛП(Строка(ИменаМакетов) + " " + Строка(ИмяМенеджераПечати)));

	Возврат СтрНайти(Текст, "факсимил") > 0
		Или СтрНайти(Текст, "facsimile") > 0
		Или СтрНайти(Текст, "сфаксимиле") > 0;

КонецФункции

&НаКлиенте
Функция Б24_ЗаказДляПроверкиДаты(МассивОбъектов, ВладелецФормы)

	Если ВладелецФормы <> Неопределено Тогда

		Попытка

			Если ЗначениеЗаполнено(ВладелецФормы.Объект.Ссылка) Тогда
				Возврат ВладелецФормы.Объект.Ссылка;
			КонецЕсли;

		Исключение
		КонецПопытки;

	КонецЕсли;

	Если МассивОбъектов = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	Если ТипЗнч(МассивОбъектов) = Тип("Массив") Тогда

		Если МассивОбъектов.Количество() = 0 Тогда
			Возврат Неопределено;
		КонецЕсли;

		ОбъектПечати = МассивОбъектов[0];

	Иначе
		ОбъектПечати = МассивОбъектов;
	КонецЕсли;

	Возврат Б24_СчетВызовСервера.ЗаказКлиентаПоОбъектуПечати(ОбъектПечати);

КонецФункции

#КонецОбласти
