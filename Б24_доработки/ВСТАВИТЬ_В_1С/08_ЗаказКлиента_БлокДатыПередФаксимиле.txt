// МОДУЛЬ ФОРМЫ: добавить в КОНЕЦ модуля заказа (заменить старую область Б24_ДатаПередПечатьюФаксимиле).
// Нужен модуль Б24_ПечатьКлиент (Клиент) + Б24_СчетВызовСервера (Сервер, Вызов сервера).
// Главный перехват — файл 10 (УправлениеПечатьюКлиент).

#Область Б24_ДатаПередПечатьюФаксимиле

&НаКлиенте
&Перед("Подключаемый_ОбработатьКоманду")
Процедура Б24_ПередПодключаемый_ОбработатьКоманду(Команда, ДополнительныеПараметры, СтандартнаяОбработка)
	Б24_БлокироватьФаксимилеБезДаты(Команда, ДополнительныеПараметры, СтандартнаяОбработка);
КонецПроцедуры

&НаКлиенте
&Перед("Подключаемый_ВыполнитьКоманду")
Процедура Б24_ПередПодключаемый_ВыполнитьКоманду(Команда, НавигационнаяСсылкаФорматированнойСтроки, СтандартнаяОбработка)
	Б24_БлокироватьФаксимилеБезДаты(Команда, Неопределено, СтандартнаяОбработка);
КонецПроцедуры

&НаКлиенте
Процедура Б24_БлокироватьФаксимилеБезДаты(Команда, ДополнительныеПараметры, СтандартнаяОбработка)

	Если НЕ Б24_ПечатьКлиент.ЭтоПечатьФаксимилеСчета(Команда, ДополнительныеПараметры) Тогда
		Возврат;
	КонецЕсли;

	ТекстОшибки = Б24_ПечатьКлиент.ТекстОшибкиДатыПередФаксимиле(Объект.Ссылка);

	Если ПустаяСтрока(ТекстОшибки) Тогда
		Возврат;
	КонецЕсли;

	ПоказатьПредупреждение(, ТекстОшибки);
	СтандартнаяОбработка = Ложь;

КонецПроцедуры

#КонецОбласти
